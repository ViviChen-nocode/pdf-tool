# NotebookLM 簡報後製工具箱

本專案提供兩大功能，所有處理皆在瀏覽器端完成，不上傳檔案：

- **PDF 轉高畫質 PNG**：將整份 PDF 逐頁輸出成高品質圖檔，方便進一步編修。
- **PDF 頁面替換**：將修改後的單頁 PNG 匯回原 PDF，產出新的 PDF。

## 環境需求
- Node.js 18+（建議搭配 npm 10+）

## 本地開發
```sh
npm install          # 安裝相依套件
npm run dev -- --host --port 8080   # 啟動開發伺服器
```
啟動後可在終端顯示的網址（預設 http://localhost:8080 或自動轉到其他埠）預覽。

## 專案腳本
- `npm run dev`：啟動本地開發伺服器（Vite）。
- `npm run build`：產出生產環境靜態檔案。
- `npm run preview`：以本地伺服器預覽 build 結果。
- `npm run lint`：執行 ESLint。

## 技術棧
- React + TypeScript
- Vite
- Tailwind CSS
- shadcn/ui 元件庫

## 開發者與回饋
- 開發者：Vivi Chen（大師姐）
- 問題回報 / 功能建議：歡迎建立 GitHub Issue，描述重現步驟與預期行為。若涉及檔案，請避免上傳敏感資訊。
